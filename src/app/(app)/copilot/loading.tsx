import { LoadingState } from "@/components/ui/state-views";

export default function CopilotLoading() {
  return <LoadingState title="Loading Knowledge Copilot" description="Preparing role-aware prompts and cited answer UI." />;
}
